@model IEnumerable<MVC_Book.Models.Book>
<!DOCTYPE html>
<html>
<body>
    <div class="responsive-container-block">
        <p class="text-blk team-head-text">
            Book List
        </p>
        @foreach (var book in Model)
        {
            <p class="text-blk name">
                @Html.DisplayFor(modelItem => book.Title)
            </p>
            <p class="text-blk">
                @Html.DisplayFor(modelItem => book.Year)
            </p>
            <p class="text-blk">
                @Html.DisplayFor(modelItem => book.Author)
            </p>
            <p class="text-blk feature-text" style="border: 2px solid #3498db; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); padding: 10px;">
                <img src="@book.CoverImage" alt="CoverImage" style="max-width: 50%; border-radius: 50%;">
            </p>
            <a asp-action="Update" asp-route-id="@book.Id" class="btn btn-primary">Update</a>
        }
    </div>
    <a asp-controller="Book" asp-action="Add">Add Book</a>
</body>
</html>